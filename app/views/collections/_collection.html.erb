<div class="lgrey m-shadow flexed p-1 m-1 cornered">
  <div class="small-post my-2 prime cornered m-shadow">
    <div class="">
    <%=link_to(image_tag(collection.image_handler(collection.image,"post"), class:"collection_image cornered"), collection,data: {turbo_frame: "_top"})%>
    </div>
    <div class="p-1 relative">
    <div class="bold"><%=link_to collection.title, collection,class:"sn-link", data:{turbo_frame:"_top"}%></div>

      <div class="tag bs"><%=collection.c_type%></div>
      <div class="abs collection-like"><%=render 'collections/components/like_button', collection:collection%></div>
    </div>
  </div>

  <div class="collection-image-grid">
  <%collection.collectibles.take(3).each do |collectible|%>
    <div class="mt-2 mx-2">
      <%=image_tag(collectible.post.image_handler(collectible.post.image,"small"), class:"cornered m-shadow") %>
    </div>
  <% end %>


    <%if collection.collectibles.count > 3 %>
    <%=link_to collection, class:"secondary-t",data: {turbo_frame: "_top"} do%>
    <div class="mt-2 mx-2 collection-last-box cornered">
      <div class="bold">+ <%=collection.collectibles.count - 3%></div>
      
      <div class="bold">See More</div>
    </div>
    <%end%>
    <%end%>
  </div>

</div>




