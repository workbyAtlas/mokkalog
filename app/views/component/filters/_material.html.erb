
  <div class="filter-grid">


    <% @materials.each do |x| %>
   <div class="my-1">
      <label class="m-checkbox">
      <%= check_box_tag('q[material_i_cont_any][]', x, false, data: { action: "change->catalog#checkboxToggle" },  oninput: 'this.form.requestSubmit();')%>
      <span class="m-checkmark"><%=x%></span>
      </label>
   </div>
    <%end%>
  </div>