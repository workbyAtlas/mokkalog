<div class="my-0 title is-5 secondary-t mb-2"><u>Style</u></div>
	<div class="flexed-wcc category-filter-grid">


	<% @styles.each do |x| %>

	<div class="my-1">
	  <label class="m-checkbox mid-cbox">
	  <%= check_box_tag('q[brand_styles_name_i_cont_any][]', x.name, false, data: { action: "change->catalog#checkboxToggle" },  oninput: 'this.form.requestSubmit();')%>
	  <span class="m-checkmark"><%=x.name%></span>
	  </label>
	</div>

	<%end%>
</div>