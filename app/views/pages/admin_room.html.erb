<div data-controller="slider" data-slider-index-value="1">
  <button data-action="slider#previous"> ← </button>
  <button data-action="slider#next"> → </button>

  <div data-slider-target="slide">🐵</div>
  <div data-slider-target="slide">🙈</div>
  <div data-slider-target="slide">🙉</div>
  <div data-slider-target="slide">🙊</div>
</div>

<%if @auth %>
<section class="section purp is-medium">
  <div class="center title is-3 has-text-white">Admin Room</div>
  <hr />
 
</section>

<section class="section">
  <%= link_to "New blog", new_blog_path, class:"button s-btn" %>
  <br>
  <ul>
    <li>User count:<%=@users.count%></li>
<% @users.each do |x| %>
  <li><%=x.email %> | <%= x.name %> | <%= x.role %></li>
<% end %>
  </ul>
</section>

<%end%>




