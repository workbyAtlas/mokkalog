
<%= render 'posts/searchbar' %>


<%# Filter & Sorters%>




<section class="section black-border flexed-wc">
  <% @posts.each do |post| %>
      <div class=""><%=render post%><%#= render "posts/post", post: post, like_status: current_user.liked?(post) %></div>
  <% end %>
</section>

<%= paginate @posts%>
<% if current_user == @auth%>
<%= render 'ahoy/chart', events: @events%>
<%end%>
<br/>
